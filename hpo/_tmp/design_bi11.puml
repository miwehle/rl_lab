@startuml

!include ../../puml_links.iuml

title BI11 - Score Trials By Their Best Checkpoint

hide footbox

participant "objective.py" as Objective
participant ":ObjectiveHooks" as Hooks
participant "Optuna Trial" as Trial
participant "checkpointing.py" as Checkpointing
participant ":Dashboard" as Dashboard
participant "robust_selection.py" as RobustSelection

url of Objective is [[WORKSPACE_ROOT/hpo/src/hpo/objective.py:147:1]]
url of Hooks is [[WORKSPACE_ROOT/hpo/src/hpo/checkpointing.py:279:1]]
url of Trial is [[https://optuna.readthedocs.io/]]
url of Checkpointing is [[WORKSPACE_ROOT/hpo/src/hpo/checkpointing.py:1:1]]
url of Dashboard is [[WORKSPACE_ROOT/hpo/src/hpo/evaluation/dashboard.py:456:1]]
url of RobustSelection is [[WORKSPACE_ROOT/hpo/src/hpo/robust_selection.py:124:1]]

Objective -> Hooks : q_net_for_evaluation(ctx)
Hooks -> Hooks : load best training-window checkpoint
Objective -> Objective : greedy evaluation
Objective -> Hooks : finalize_trial(ctx)
Hooks -> Trial : set evaluation_checkpoint_score\nset evaluation_checkpoint_path

box "BI11 change" #FFF2CC
    Dashboard -> Checkpointing : trial_best_checkpoint_score(trial)
    Checkpointing -> Trial : read evaluation_checkpoint_score\nfallback trial.value
    Checkpointing --> Dashboard : score for Study dot and best line

    RobustSelection -> Checkpointing : trial_best_checkpoint_score(trial)
    Checkpointing -> Trial : read evaluation_checkpoint_score\nfallback trial.value
    Checkpointing --> RobustSelection : score for candidate sorting
end box

@enduml
